# catkin build products
build/
devel/

# python
__pycache__/
*.pyc

# local scratch: timestamped route backups, pre-edit copies, model backups
*.bak
*_orig
.mass_backup/

# runtime logs, except the ones shipped with the published results
*.log
!results/**/*.log

# catkin creates this as a symlink into /opt/ros; catkin_init_workspace remakes it
src/CMakeLists.txt
